Build the desktop app and report any errors.

1. `cd desktop && npm run build`
2. If build fails, read the error output carefully and identify the root cause
3. If build succeeds, confirm the output files in `desktop/dist/`

Do NOT fix errors automatically — report them first so we can decide the approach.

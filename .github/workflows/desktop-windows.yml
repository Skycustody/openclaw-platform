name: Build Desktop (Windows)

on:
  workflow_dispatch:
    inputs:
      version:
        description: 'Release version tag (e.g. v0.1.0)'
        required: true
        default: 'v0.1.0'

jobs:
  build-windows:
    runs-on: windows-latest
    defaults:
      run:
        working-directory: desktop

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22

      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install Python setuptools (needed by node-gyp)
        run: pip install setuptools

      - name: Install dependencies
        run: npm ci || npm install

      - name: Build TypeScript
        run: npm run build

      - name: Build Windows installer
        run: npx electron-builder --win --publish never

      - name: Upload to release
        env:
          GH_TOKEN: ${{ secrets.GH_RELEASE_TOKEN }}
        run: |
          $exe = Get-ChildItem -Path release -Filter "Valnaa-Setup.*" -Exclude "*.blockmap","*.yaml" | Select-Object -First 1
          if ($exe) {
            Write-Host "Uploading $($exe.Name) ($([math]::Round($exe.Length / 1MB, 1)) MB)"
            gh release upload ${{ github.event.inputs.version }} $exe.FullName --repo Skycustody/valnaa-desktop --clobber
          } else {
            Write-Error "No Valnaa-Setup installer found in release/"
            exit 1
          }
